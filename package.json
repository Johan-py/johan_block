{
  "name": "var-log-johan-py",
  "type": "module",
  "version": "1.0.0",
  "description": "/var/log/Johan-py — Bitácora de un Sysadmin",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "pnpm run build && wrangler dev",
    "deploy": "pnpm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.96.0"
  }
}
